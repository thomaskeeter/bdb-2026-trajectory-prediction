# torch below is the CPU-only build. On Linux, install it first with:
#   pip install torch==2.14.0 --index-url https://download.pytorch.org/whl/cpu
# otherwise pip pulls the default (CUDA) build. Then: pip install -r requirements.txt
numpy==2.5.3
pandas==3.0.5
torch==2.14.0
